name: flutter_libphonenumber_web_example
description: Demonstrates how to use the flutter_libphonenumber_web plugin.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_libphonenumber_web:
    path: ../
  flutter_libphonenumber_platform_interface: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
